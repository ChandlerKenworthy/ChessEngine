#include <iostream>
#include <bitset>
#include <vector>

#include "Constants.hpp"
#include "Board.hpp"
#include "Engine.hpp"

int main() {
    //Board board = Board();
    //Engine e = Engine(false); // turns off loading arrays from file for slight speedup when testing
    //board.GenerateAttackTables();

    //std::cout << get_LSB(RANK_1 & FILE_A) << std::endl;
    //std::cout << get_LSB(RANK_1 & FILE_H) << std::endl;
    //std::cout << get_LSB(RANK_2 & FILE_H) << std::endl;
    //std::cout << get_LSB(RANK_8 & FILE_A) << std::endl;

    //std::cout << e.GetWhitePawnPosValue(RANK_1 & FILE_A) << std::endl;
    //std::cout << e.GetWhitePawnPosValue(RANK_2 & FILE_A) << std::endl;
    //std::cout << e.GetWhitePawnPosValue(RANK_3 & FILE_B) << std::endl;
    //PrintBitset(board.GetBoard(Color::White, Piece::Knight));

    //U64 x = RANK_5 & FILE_D;


    // board.GenerateBishopAttacks(get_LSB(b), b);
    //board.GenerateAttackTables();

    //float x = e.Evaluate(&board);
    //std::cout << "Evaluation = " << x << std::endl;
    //PrintBitset(b);
    //PrintBitset(board.GetQueenAttacks(b));

    // PrintBitset(board.GetKnightAttacks(b));
}